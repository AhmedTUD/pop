# مشروع POP Materials Management System

نظام إدارة مواد POP (Point of Purchase) لشركة Samsung - نظام شامل لتتبع وإدارة مواد العرض في المتاجر.

## المميزات

- 🔐 نظام تسجيل دخول آمن للموظفين والإدارة
- 📊 إدخال وتتبع بيانات مواد POP
- 🖼️ رفع وإدارة الصور
- 📈 تقارير شاملة وتصدير Excel
- 🏪 إدارة الفروع والمتاجر
- 📱 واجهة متجاوبة تعمل على الهواتف
- 🔄 نسخ احتياطية تلقائية
- 📊 لوحة تحكم إدارية متقدمة

## التقنيات المستخدمة

- **Backend**: Python Flask
- **Database**: SQLite (مع دعم PostgreSQL)
- **Frontend**: HTML, CSS, JavaScript
- **Containerization**: Docker & Docker Compose
- **Web Server**: Nginx (Reverse Proxy)
- **SSL**: Let's Encrypt (Certbot)

## متطلبات النظام

- Ubuntu/Debian Linux Server
- Docker & Docker Compose
- Nginx
- 2GB RAM (الحد الأدنى)
- 10GB مساحة تخزين

## التثبيت والنشر

### النشر السريع

```bash
# 1. استنساخ المشروع
git clone https://github.com/AhmedTUD/pop.git
cd pop

# 2. إعداد السيرفر (مرة واحدة فقط)
chmod +x setup-server.sh
sudo ./setup-server.sh

# 3. إعداد البيئة
cp .env.docker .env
nano .env  # عدل SECRET_KEY والإعدادات

# 4. نشر التطبيق
chmod +x deploy-production.sh
./deploy-production.sh

# 5. إعداد Nginx والدومين
sudo cp nginx.conf /etc/nginx/sites-available/pop-materials
sudo nano /etc/nginx/sites-available/pop-materials  # عدل الدومين
sudo ln -s /etc/nginx/sites-available/pop-materials /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx

# 6. الحصول على شهادة SSL
sudo certbot --nginx -d your-domain.com

# 7. إعداد المراقبة التلقائية
chmod +x crontab-setup.sh
sudo ./crontab-setup.sh
```

### للمطورين (التطوير المحلي)

```bash
# استنساخ المشروع
git clone https://github.com/AhmedTUD/pop.git
cd pop

# تثبيت المتطلبات
pip install -r requirements.txt

# إعداد متغيرات البيئة
cp .env.docker .env

# تشغيل التطبيق
python app.py
```

## الاستخدام

### حسابات افتراضية

- **المدير**: 
  - اسم المستخدم: `Admin`
  - كلمة المرور: `admin123`

### الوصول للنظام

- **التطبيق**: https://your-domain.com
- **لوحة الإدارة**: تسجيل دخول بحساب المدير

## إدارة النظام

### أوامر مفيدة

```bash
# عرض حالة التطبيق
docker-compose -f docker-compose.prod.yml ps

# عرض اللوجز
docker-compose -f docker-compose.prod.yml logs -f

# إعادة تشغيل
docker-compose -f docker-compose.prod.yml restart

# تحديث التطبيق
git pull
./deploy-production.sh

# مراقبة النظام
./monitor.sh status
```

### النسخ الاحتياطية

النظام يقوم بإنشاء نسخ احتياطية تلقائية:
- **يومياً**: نسخة احتياطية لقاعدة البيانات (2:00 ص)
- **أسبوعياً**: نسخة احتياطية للصور (الأحد 3:00 ص)
- **شهرياً**: تنظيف النظام وحذف الملفات القديمة

مجلد النسخ الاحتياطية: `/opt/backups/pop-materials/`

### المراقبة

النظام يتضمن مراقبة تلقائية لـ:
- حالة التطبيق (كل 5 دقائق)
- استخدام الموارد
- مساحة القرص الصلب
- الأخطاء في اللوجز
- إعادة تشغيل تلقائية عند الحاجة

## هيكل المشروع

```
pop/
├── app.py                    # التطبيق الرئيسي
├── requirements.txt          # متطلبات Python
├── Dockerfile               # إعداد Docker
├── docker-compose.yml       # إعداد Docker Compose للتطوير
├── docker-compose.prod.yml  # إعداد Docker Compose للإنتاج
├── nginx.conf              # إعداد Nginx
├── deploy-production.sh    # سكريبت النشر
├── monitor.sh             # سكريبت المراقبة
├── setup-server.sh        # سكريبت إعداد السيرفر
├── crontab-setup.sh       # إعداد المهام التلقائية
├── static/                # الملفات الثابتة
│   ├── css/
│   ├── js/
│   └── uploads/          # الصور المرفوعة
├── templates/            # قوالب HTML
└── excel_export_enhanced.py  # تصدير Excel
```

## الأمان

- 🔐 تشفير كلمات المرور
- 🛡️ حماية من CSRF
- 🔒 HTTPS إجباري
- 🚫 تحديد حجم الملفات المرفوعة
- 🔍 تسجيل جميع العمليات
- 🛡️ إعدادات أمان Nginx متقدمة

## المساهمة

1. Fork المشروع
2. إنشاء branch جديد (`git checkout -b feature/AmazingFeature`)
3. Commit التغييرات (`git commit -m 'Add some AmazingFeature'`)
4. Push للـ branch (`git push origin feature/AmazingFeature`)
5. فتح Pull Request

## الترخيص

هذا المشروع مرخص تحت رخصة MIT - راجع ملف [LICENSE](LICENSE) للتفاصيل.

## الدعم

إذا واجهت أي مشاكل:

1. تحقق من [دليل استكشاف الأخطاء](README_DOCKER.md#استكشاف-الأخطاء)
2. راجع اللوجز: `docker-compose logs`
3. تحقق من حالة النظام: `./monitor.sh status`
4. أنشئ Issue في GitHub

## معلومات الاتصال

- **المطور**: Ahmed TUD
- **البريد الإلكتروني**: [your-email@example.com]
- **GitHub**: [AhmedTUD](https://github.com/AhmedTUD)

---

**ملاحظة**: هذا المشروع تم تطويره لشركة Samsung لإدارة مواد POP في المتاجر.