# نظام إدارة مواد POP - Samsung

نظام إدارة مواد POP (Point of Purchase) لشركة Samsung - نظام شامل لتتبع وإدارة مواد العرض في المتاجر.

## المميزات الرئيسية

- 🔐 نظام تسجيل دخول آمن للموظفين والإدارة
- 📊 إدخال وتتبع بيانات مواد POP
- 🖼️ رفع وإدارة الصور
- 📈 تقارير شاملة وتصدير Excel
- 🏪 إدارة الفروع والمتاجر
- 📱 واجهة متجاوبة تعمل على الهواتف
- 📊 لوحة تحكم إدارية متقدمة

## التقنيات المستخدمة

- **Backend**: Python Flask
- **Database**: SQLite (مع دعم PostgreSQL)
- **Frontend**: HTML, CSS, JavaScript
- **Containerization**: Docker

## التثبيت والتشغيل

### التطوير المحلي

```bash
# 1. استنساخ المشروع
git clone <repository-url>
cd pop-materials

# 2. تثبيت المتطلبات
pip install -r requirements.txt

# 3. إعداد متغيرات البيئة
cp .env.example .env
# عدل الإعدادات في ملف .env حسب الحاجة

# 4. تشغيل التطبيق
python app.py
```

### باستخدام Docker

```bash
# 1. بناء وتشغيل التطبيق
docker-compose up --build

# 2. الوصول للتطبيق
# http://localhost:5000
```

## الاستخدام

### حساب المدير الافتراضي

- **اسم المستخدم**: `Admin`
- **كلمة المرور**: `admin123`

### الوظائف الأساسية

1. **تسجيل الدخول**: استخدم حساب المدير أو أي حساب موظف
2. **إدخال البيانات**: إضافة بيانات مواد POP للمتاجر
3. **إدارة المستخدمين**: إضافة وإدارة حسابات الموظفين
4. **التقارير**: عرض وتصدير التقارير بصيغة Excel
5. **إدارة البيانات**: إدارة الفئات والموديلات ومواد POP

## هيكل المشروع

```
├── app.py                      # التطبيق الرئيسي
├── requirements.txt            # متطلبات Python
├── database_config.py          # إعدادات قاعدة البيانات
├── database_manager.py         # إدارة قاعدة البيانات
├── excel_export_enhanced.py    # تصدير Excel
├── wsgi_config.py             # إعداد WSGI
├── Dockerfile                 # إعداد Docker
├── docker-compose.yml         # إعداد Docker Compose
├── static/                    # الملفات الثابتة (CSS, JS, الصور)
├── templates/                 # قوالب HTML
└── database.db               # قاعدة البيانات (SQLite)
```

## الأمان

- تشفير كلمات المرور باستخدام Werkzeug
- حماية من رفع ملفات ضارة
- تحديد حجم الملفات المرفوعة
- جلسات آمنة

## المساهمة

1. Fork المشروع
2. إنشاء branch جديد للميزة الجديدة
3. Commit التغييرات
4. Push للـ branch
5. فتح Pull Request

## الترخيص

هذا المشروع مرخص تحت رخصة MIT - راجع ملف [LICENSE](LICENSE) للتفاصيل.

---

**تم تطوير هذا المشروع لشركة Samsung لإدارة مواد POP في المتاجر**